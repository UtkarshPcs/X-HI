/**
 * NotificationPrompt.jsx — thin compatibility stub.
 * Logic moved to src/ux/components/NotificationPromptBanner.jsx (mounted via UXRenderer).
 * Kept so the existing import in App.jsx still resolves.
 */

export default function NotificationPrompt() {
  return null;
}
