/**
 * ForegroundToast.jsx — thin compatibility stub.
 * FCM foreground push toasts now go through ToastContainer (mounted via UXRenderer).
 * Kept so the existing import in App.jsx still resolves.
 */

export default function ForegroundToast() {
  return null;
}
