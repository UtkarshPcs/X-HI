/**
 * WhatsNew.jsx — thin compatibility stub.
 * Modal + tour logic has moved to UXRenderer via CampaignModal + TourRunner.
 * Kept so existing import in App.jsx still resolves without errors.
 */

export default function WhatsNew() {
  // All logic moved to src/ux/components/UXRenderer.jsx
  return null;
}
