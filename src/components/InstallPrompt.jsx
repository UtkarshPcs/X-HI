/**
 * InstallPrompt.jsx — thin compatibility stub.
 * Logic moved to src/ux/components/InstallPromptWrapper.jsx (mounted via UXRenderer).
 * Kept so the existing import in App.jsx still resolves.
 */

export default function InstallPrompt() {
  return null;
}
